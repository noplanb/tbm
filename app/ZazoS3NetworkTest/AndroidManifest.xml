<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.zazoapp.s3networktest"
          android:versionCode="1"
          android:versionName="1.0">
    <uses-sdk android:minSdkVersion="14"
            android:targetSdkVersion="22"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application android:label="@string/app_name" android:icon="@drawable/ic_launcher">
        <activity android:name="MyActivity"
                  android:label="@string/app_name"
                  android:launchMode="singleInstance"
                  android:configChanges="orientation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <service android:name=".core.IntentHandlerService"/>

        <service
                android:name=".network.FileUploadService" />
        <!-- We can run FTS in the same process as app. I have tested and the notstop intent service restarts fine even after
             power cycle. Putting it in the same process allows models to be shared in memory. -->
        <!-- android:process=":FileTransferService" -->
        <service
                android:name=".network.FileDownloadService" />
        <!-- android:process=":FileTransferService" -->
        <service
                android:name=".network.FileDeleteService" />
        <service android:name=".ManagerService">
            <intent-filter>
                <action android:name="com.zazoapp.s3networktest.ManagerService.ACTION_START"/>
                <action android:name="com.zazoapp.s3networktest.ManagerService.ACTION_STOP"/>
                <action android:name="com.zazoapp.s3networktest.ManagerService.ACTION_RESET"/>
                <action android:name="com.zazoapp.s3networktest.ManagerService.ACTION_UPDATE_INFO"/>
            </intent-filter>
        </service>
    </application>
</manifest>
