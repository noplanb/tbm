<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zazoapp.client">

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.camera.front" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" /> <!-- imply camera, autofocus features -->
    <!--<uses-permission android:name="com.sonyericsson.permission.CAMERA_EXTENDED"/>-->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAPTURE_VIDEO_OUTPUT" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" /> <!--Normal permission-->
    <uses-permission android:name="android.permission.INTERNET" /> <!--Normal permission-->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!--Normal permission-->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_LOGS"/>
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>  <!--Normal permission-->

    <permission
        android:name="com.zazoapp.client.permission.C2D_MESSAGE"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.zazoapp.client.permission.C2D_MESSAGE" />
    <uses-permission android:name="com.google.android.c2dm.SEND" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.SEND_SMS"/> <!-- imply telephony feature -->
    <uses-permission android:name="android.permission.READ_SMS"/> <!-- imply telephony feature -->
    <uses-permission android:name="android.permission.READ_PROFILE"/>

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/mybackupscheme"
        android:icon="@drawable/ic_launcher"
        android:logo="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" 
        android:name=".core.TbmApplication">

        <receiver
                android:name="com.appsflyer.MultipleInstallBroadcastReceiver"
                android:exported="true">
            <intent-filter>
                <action android:name="com.android.vending.INSTALL_REFERRER"/>
            </intent-filter>
        </receiver>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleInstance"
            android:windowSoftInputMode="stateAlwaysHidden|adjustNothing"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http"
                      android:host="ff.zazoapp.com"
                      android:pathPrefix="/w/" />
                <data android:scheme="https"
                      android:host="ff.zazoapp.com"
                      android:pathPrefix="/w/" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.LockScreenAlertActivity"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.LockScreen" />
        <activity
                android:name=".debug.DebugSettingsActivity"
                android:launchMode="singleTask"
                android:screenOrientation="portrait"
                android:theme="@style/DebugTheme"/>
        <activity
                android:name=".ui.WelcomeScreenActivity"
                android:launchMode="singleTask"
                android:screenOrientation="portrait"
                android:windowSoftInputMode="stateAlwaysHidden|adjustNothing"
                android:theme="@style/AppTheme"
                />
        <receiver
            android:name=".notification.gcm.GcmBroadcastReceiver"
            android:permission="com.google.android.c2dm.permission.SEND" >
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />

                <category android:name="com.zazoapp.client" />
            </intent-filter>
        </receiver>

        <service android:name=".notification.gcm.GcmIntentService" />

        <service android:name=".core.IntentHandlerService"/>

        <service
            android:name=".network.FileUploadService" />
       		<!-- We can run FTS in the same process as app. I have tested and the notstop intent service restarts fine even after 
       		     power cycle. Putting it in the same process allows models to be shared in memory. -->
            <!-- android:process=":FileTransferService" -->
        <service
            android:name=".network.FileDownloadService" />
            <!-- android:process=":FileTransferService" -->
        <service
            android:name=".network.FileDeleteService" />
            <!-- android:process=":FileTransferService" -->

        <receiver android:name=".core.BootCompleteReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <receiver android:name=".core.IntentHandlerService$ShutdownReceiver">
            <intent-filter>
                <action android:name="android.intent.action.ACTION_SHUTDOWN" />
            </intent-filter>
        </receiver>
        <receiver android:name="com.appsflyer.AppsFlyerLib">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_REMOVED"/>
                <data android:scheme="package"/>
            </intent-filter>
        </receiver>
        <receiver android:name=".core.DateChangedReceiver">
            <intent-filter>
                <action android:name="android.intent.action.DATE_CHANGED"/>
            </intent-filter>
        </receiver>
        <!-- For tests -->
        <!--<activity android:name=".tests.TestActivity" android:screenOrientation="portrait" android:launchMode="singleTask"/>-->
        <!--<service android:name=".tests.FileDownloadServiceTest"/>-->
        <!--<service android:name=".tests.FileUploadServiceTest"/>-->
    </application>

</manifest>