<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
                android:id="@+id/navigation_item_attachment"
                android:checked="true"
                android:icon="@drawable/ic_action_view"
                android:title="@string/action_view" />
        <item
                android:id="@+id/navigation_item_images"
                android:icon="@drawable/ic_camera_switch"
                android:title="@string/feature_switch_camera_action" />
        <item
                android:id="@+id/navigation_item_location"
                android:icon="@drawable/ic_arrow_back"
                android:title="@string/abc_search_hint" />
    </group>

    <item android:title="Submenu">
        <menu>
            <item
                    android:icon="@drawable/ic_flag_us"
                    android:title="US" />
            <item
                    android:icon="@drawable/ic_flag_ua"
                    android:title="UA" />
        </menu>
    </item>

</menu>