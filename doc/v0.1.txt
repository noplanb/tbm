Todo
  
  App
    x User model
    x Get user on startup.
    x first, last, id, registered
    
      x Registered?
      
    x Friend model
      x First, last, id, view_index
      x Find, find_by_view_index, video_path, video_played
      x Save model in file.
      x Retrieve from file.
    
      
    x Startup
      x Sequence
        x Try to retrieve config else create
        x Try to retrieve friends
        x If not registered user start RegisterActivity
          x Pull signup list from server
          x Add self to config
          x Add friends
        x Back to HomeActivity
        x Draw the ui with him and his friends.
        x Load saved models
        x Show UI based on models
          
    x Shutdown
      x Save data
        x Save config
        x Save friends
        
    x Config
      x server_uri
      
    x UI for unregistered user startup.
      
    x Server communication
      - Http gets and posts
      x upload video to paperclip
        - user_id, recipient_id
      - download video
        - Delete on server after successful download
      - Send app errors to server
      
    x UI
      - Recording indicator broken again.
      - Unplayed video indicator
      - Show faces before play
      - Logo
      
    - Notifications
      x Upload video in background
      - Show unplayed.
      - Fix notification
    
    - Video play and record
      - Replay your last
    
  
  Server
    - Save push token
    - Send notifications
    - Save video
    x Register
      x user_list
        x list of ids, first, last
      x pick_user/id
        x list of friends
          x id, first, last
    
    
Post release
  - Make the upload service wakeful
